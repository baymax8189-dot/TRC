services:
  - type: worker
    name: chartlink-scraper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python cloud_scraper.py
    envVars:
      - key: DATABASE_URL
        sync: false
